version: '3.8'
services:
  dojo-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dojo-frontend
    restart: always
    ports:
      - "3001:3000"
    environment:
      # IMPORTANTE: Aquí le decís al cliente dónde está tu API
      NEXT_PUBLIC_API_URL: http://localhost:3000
